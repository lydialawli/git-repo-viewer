import React from 'react'

const Search = () => {

  return (
    <div >
      <input onChange={() => { }} className='searchBar' type='text' placeholder={'Search...'} />
    </div>
  )
}

export default Search